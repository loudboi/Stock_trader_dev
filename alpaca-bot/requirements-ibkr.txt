# IBKR path only. Install alongside requirements.txt.
# Major-version upper bounds avoid silently crossing an untested breaking release;
# record `python -m pip freeze` with any result/deployment for exact reproduction.
ib_async>=2.0,<3
tzdata>=2024.1,<2027
