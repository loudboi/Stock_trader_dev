name: Alternative alpha low-turnover regime

on:
  push:
    branches:
      - agent/alternative-alpha-research
  pull_request:
    branches:
      - agent/tax-alpha-research
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: alpaca-bot
        shell: bash
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: |
            alpaca-bot/requirements.txt
            alpaca-bot/requirements-backtest.txt
      - name: Install dependencies
        run: python -m pip install -r requirements.txt -r requirements-backtest.txt
      - name: Run fixed low-turnover regime
        run: |
          set -o pipefail
          PYTHONPATH=. python research/alternative_alpha_low_turnover_regime.py | tee alternative_alpha_low_turnover_results.txt
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: alternative-alpha-low-turnover
          if-no-files-found: error
          path: |
            alpaca-bot/alternative_alpha_low_turnover_results.txt
            alpaca-bot/alternative_alpha_low_turnover_path.csv
            alpaca-bot/alternative_alpha_low_turnover_summary.json
